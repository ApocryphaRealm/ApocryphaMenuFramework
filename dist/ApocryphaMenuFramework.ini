; Apocrypha Menu Framework - settings. Rewritten by the in-game settings page;
; edits made here while the game is closed are honoured on the next load.

[Input]
; DirectInput scan code that toggles the framework menu. 59 (0x3B) = F1.
uToggleKey=59
; 0 = keyboard navigation, 1 = controller navigation.
bControllerMode=0
; 1 = follow whatever you last used: press a key or move the mouse and the menu switches to
; keyboard navigation; touch the pad and it switches to controller navigation. 0 = the
; setting above is obeyed exactly (the default - an explicit choice never drifts).
bAutoInputMode=0

[Display]
; Extra text scale on top of the automatic resolution scaling.
fTextScale=1.3
; Window position preset. 0 = centre. Preset anchors, not free placement.
uWindowPreset=0

[Log]
; 0 = trace (most comprehensive, the project default) ... 6 = off.
uLogLevel=0

[Debug]
; 1 shows the AMF API Demo menu (registered through the public API).
bShowApiDemo=1

[MenuAlias]
; Player-facing names for mods' menu entries: <mod's registered name>=<what to show>.
; Set from the Framework Settings page; the mod itself is untouched.

[MenuOrder]
; 0 = alphabetical (the default), 1 = the custom sequence below.
bCustomOrder=0
; The custom sequence, pipe-separated, in list order. Typing a position number on
; the Framework Settings page rewrites this; mods missing from it insert at their
; alphabetical position.
sOrder=
